# MongoDB connection string
MONGO_URI=mongodb://localhost:27017/calm

# Flask secret key (for sessions, JWT, etc.)
SECRET_KEY=your-very-secret-key

# (Optional) Flask environment
FLASK_ENV=development

# (Optional) Port number
PORT=5000